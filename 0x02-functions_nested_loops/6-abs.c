#include "main.h"

/**
 * _abs - function Computes the absolute value of an integer.
 * @n: The integer 
 *
 * Return: The absolute of value 
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
