/**
 * print_numbers - function that prints the numb * ers, from 0 to 9
 * main - check the code
 * Return: Always 0.
 */
void print_numbers(void)
{
int x;
while (x >= 0 && x <= 9)
	}
	num++;
	_putchar(x + 48)
	}
putchar('\n');
}
